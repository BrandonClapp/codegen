Directory for sample templates

Template input directory is defined in `gen.config.json` under `templates[].inDir`

```json
{
  "templates": [
    {
      "inDir": "../templates/entity"
      //...other props
    }
  ]
}
```
